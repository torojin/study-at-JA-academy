package emp;
//VO(Value Object), entity(행), DTO(data transfer object), bean
//		: 데이터를 하나로 묶어서 전송할 목적으로 만든 클래스
//getter,setter까지 만든게 VO라고 불린다.
public class EmpVO {
	//field
	private String ename;
	private String dept;
	private int salary;
	//default constructor
	
	
}
